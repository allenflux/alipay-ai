# Install the matching CUDA build of PyTorch/TorchVision separately FIRST.
# See README: installing torch from PyPI on Windows may select a CPU-only wheel.
# PaddlePaddle itself is also platform-specific; see README before enabling PaddleOCR.
numpy>=1.26
opencv-python>=4.10
Pillow>=10.3
tqdm>=4.66
